# GLStation
A powergrid simulator that stimulates the powergrid.

**Dependencies**
- libcurl
- c++20
- winhttp (if on windows)
- cmake

## Build Instructions

``` bash
git clone https://github.com/jazz-club/GLStation.git
cd GLStation/
make
```